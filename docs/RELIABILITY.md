# RELIABILITY

这个文件用于记录可靠性要求，但当前不预设具体系统方案。

## 建议记录维度

- 画布对象状态是否会无声丢失
- Webview 与宿主之间的状态同步是否可恢复
- 终端、Agent 或任务失败时是否可见
- workspace 重载或扩展重启后是否能恢复关键上下文
- 外部依赖或 AI 服务失败时如何降级

## 使用原则

- 先定义可靠性目标，再写实现手段。
- 先明确失败类型，再讨论恢复策略。
- 如果某个可靠性结论来自实验，应标注来源和证据。
