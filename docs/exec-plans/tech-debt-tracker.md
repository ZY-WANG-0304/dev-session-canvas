# 技术债追踪

本文件用于记录复杂任务完成后遗留、但当前不阻塞交付的问题。

## 记录字段

- 日期
- 主题
- 背景与触发条件
- 影响范围
- 当前临时处理
- 建议修复时机
- 关联文档或代码路径

## 技术债列表

| 日期 | 主题 | 背景与触发条件 | 影响范围 | 当前临时处理 | 建议修复时机 | 关联文档或代码路径 |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-04-19 | 历史已选定设计文档尚未迁移到 `正式方案` 结构 | 本轮更新了 `docs/DESIGN.md` 与设计文档模板，明确新写或本次改动到的 `decision_status: 已选定` 文档应使用 `正式方案` 章节收口，并在方案说明中按需体现结构化细节；但仓库里已有的历史 `已选定` 文档大多仍沿用 `当前结论` 结构，未在同一轮完成批量迁移。 | `docs/design-docs/` 下历史 `已选定` 文档的结构一致性，以及协作者判断“哪些文档已经按新规范收口、哪些仍处于待迁移状态”的成本。 | 当前采用显式过渡规则：只强制新写或本次改动到的 `已选定` 文档使用 `正式方案` 章节；未改动的历史文档继续保留现状，但统一视为待迁移技术债，而不是已完成规范收口。 | 后续当某份历史 `已选定` 设计文档再次被修改时，优先顺手迁移到新结构；若后续安排专门的文档治理轮，也可集中完成批量迁移。 | `docs/DESIGN.md`、`docs/design-docs/template-design-doc.md`、`docs/design-docs/index.md` |
| 2026-04-18 | 节点标题连续提交仍可能被乱序宿主回写短暂覆盖 | 本轮为修复“Enter 提交后旧值回灌”和“IME 确认 Enter 重复显示”，在 `ChromeTitleEditor` 里引入了单值 `pendingTitleRef`。这已经覆盖单次 in-flight 提交，但如果宿主 `host/stateUpdated` 明显慢于用户连续两次改名，前一次较旧的宿主回写仍可能在后一条提交确认前短暂把输入框显示拉回旧标题。 | `Agent` / `Terminal` / `Note` 标题输入框在高延迟宿主回写下的显示连续性，尤其影响连续两次快速改名时的 UI 稳定感知。 | 当前实现只承诺单次 pending 提交不会被本地 `blur` / Enter 提交流程立刻重置回旧值，并通过 Playwright 回归覆盖普通 Enter 与 IME 确认 Enter；暂不把“多次连续提交下的乱序宿主回写隔离”写成已解决。 | 下一轮如果继续收口标题编辑鲁棒性，或宿主状态回写链路出现更高延迟、需要支持多次并发标题提交时，应把 pending title 从单值 ref 升级为带序号的 in-flight 提交队列或版本戳。 | `src/webview/main.tsx`、`tests/playwright/webview-harness.spec.mjs`、`docs/design-docs/note-only-auxiliary-node-and-theme-alignment.md` |
| 2026-04-18 | 执行节点 file-like link refine 仍有少量前后文误并入 | 本轮已经收窄 `search` fallback，避免把普通 token 都注册成 `Search workspace` link，并补了中文前缀与中式标点的裁剪；但人工验证仍确认，少量“说明文字紧贴路径”的终端输出会把前缀或后缀文本一起吞进 file-like candidate，最终以 `search` link 形态暴露出来。 | `Terminal` / `Agent` 节点里少量复杂终端输出的文件路径识别准确率，尤其影响中文 prose、长路径、缺分隔符或路径前后直接粘连自然语言的场景。 | 当前实现已经把误报范围收敛到“少量 file-like candidate refine 误判”，并通过 Webview 回归覆盖“普通词条不再被注册成 link”“中文前缀裁剪”和“不存在路径走 Quick Access search”；仓库明确不把 file detection coverage 写成与原生 Terminal 完全等价。 | 下一轮若继续收口 execution link parity，或需要引入更强的终端上下文/分词线索、对照更多 VSCode Terminal 边界样例时 | `src/common/executionTerminalLinks.ts`、`src/webview/executionTerminalNativeInteractions.ts`、`tests/playwright/webview-harness.spec.mjs`、`docs/design-docs/execution-node-terminal-native-interactions.md` |
| 2026-04-17 | 沙箱环境下 Node `spawnSync("git", ...)` 会误附 `EPERM` | 在当前 Codex 沙箱里，shell 直接执行 `git` 正常，但 Node 侧 `child_process.spawnSync('git', ...)` 会出现“`status = 0` 且命令实际成功，同时附带 `error.code = 'EPERM'`”的返回形态。随着 `package:vsix` 与 clean-checkout 验证链路新增 README final-ref git 校验，这个环境兼容性问题会让沙箱内的发布校验入口表现得不稳定。 | 沙箱内所有通过 Node 调 git 的发布校验脚本，尤其是 `scripts/package-vsix.mjs` 与 `scripts/run-clean-checkout-vsix-validation.mjs`；可能把已经成功的 git 校验误判成失败。 | 当前不在这轮 `0.1.1` 发布准备里继续修复；发布前与实际发布仍统一在非沙箱环境执行 `npm run package:vsix` 和 `node scripts/run-clean-checkout-vsix-validation.mjs --ref <final-ref>`，并以非沙箱验证结果为准。 | 后续如果要提升 Codex 沙箱内发布校验链路的稳定性，或准备把这条校验更广泛地下放到受限执行环境时 | `scripts/package-vsix.mjs`、`scripts/run-clean-checkout-vsix-validation.mjs`、`docs/public-preview-release-playbook.md` |
| 2026-04-15 | serialized terminal snapshot 在尺寸漂移下仍缺无损重绘语义 | 本轮已经用 serialized terminal state 取代 raw tail replay，并修复了 Editor / Panel 标签切换与 Webview 重建后的终端空白；但验证中也确认，xterm 对 alternate-buffer snapshot 做 hydrate 后如果立刻按更小容器 destructive `fit()`，会裁掉顶部行。保活切回路径上的无条件 `fit()` 也会把 retain 下的现存 viewport 改写成更少行数，因此当前实现已经把 visibility restore 收口为 non-destructive redraw。 | 当节点尺寸、字体度量或宿主 CSS 变化导致“当前容器尺寸”和“snapshot 记录尺寸”不一致时，Webview 重建后的排版精度与用户手动滚动 viewport 的精确保真。 | 当前把同一宿主标签切换主路径交给 `retainContextWhenHidden` + visibility restore 的 non-destructive redraw，把重建路径交给 serialized terminal state hydrate；不把“任意尺寸漂移下的无损重绘”写成已解决。 | 下一轮如果要继续收口 Webview 重建语义、支持更强的 scrollback / viewport 恢复，或升级 xterm 恢复能力时 | `src/common/serializedTerminalState.ts`、`src/panel/CanvasPanelManager.ts`、`src/supervisor/runtimeSupervisorMain.ts`、`src/webview/main.tsx`、`tests/playwright/webview-harness.spec.mjs`、`docs/design-docs/embedded-terminal-runtime-window.md`、`docs/exec-plans/active/runtime-terminal-state-restore.md` |
| 2026-04-15 | Marketplace README 真实宿主素材导出当前仍依赖 Linux X11 工具链 | 为了把正式 `PNG` / `MP4` / `GIF` 收口到真实 VS Code 宿主窗口，本轮脚本使用 `@vscode/test-electron` + `Xvfb` + `xwininfo` + `ffmpeg` 在 Linux/X11 环境下自动抓帧。这样可以保证最终画面来自真实宿主，但也把素材导出与特定桌面工具链绑定在一起。 | 跨平台可移植性和未来 CI 化能力，尤其影响 macOS / Windows 环境是否能直接重跑同一条素材导出命令。 | 当前把 `npm run generate:marketplace-media` 定位为维护者脚本，优先保证 Linux 无头环境可重复导出；不把它写成跨平台、零依赖能力。 | 未来如果要把素材导出接入 CI，或要求 macOS / Windows 协作者也能直接生成 Marketplace 物料时 | `scripts/generate-marketplace-media.mjs`、`scripts/vscode-smoke-runner.mjs`、`tests/vscode-smoke/marketplace-media-tests.cjs`、`docs/design-docs/marketplace-readme-media-automation.md`、`docs/exec-plans/completed/marketplace-real-vscode-media-automation.md` |
| 2026-04-14 | Linux / `Remote SSH` 的 `systemd-user` 主路径仍缺真实长断开人工验收 | 本轮已经用 `fake-systemd` reopen smoke、本机 `systemd-user-real-reopen` / `systemd-fallback-real-reopen` 场景与 backend / guarantee 断言闭合主实现，但还没有在真实 Linux + `Remote SSH` 环境中做“断开数分钟后再回来”的人工验收。 | `systemd-user` 路线何时可以从“验证中”升级为“已验证”的发布口径，尤其影响 Preview 阶段对强保证 `live-runtime` 的人工信心。 | 当前继续把设计文档标为 `验证中`，不把 Linux / `Remote SSH` 下的强保证写成已验证；PR 级别仍以本地 reopen smoke 与短链路证据为准。 | 下一轮准备把 Linux / `Remote SSH` 的 `live-runtime` 口径升级为“已验证”，或需要做 Preview 阶段真实长断开验收时 | `scripts/run-vscode-smoke.mjs`、`tests/vscode-smoke/real-reopen-tests.cjs`、`docs/design-docs/runtime-persistence-and-session-supervisor.md`、`docs/exec-plans/completed/runtime-host-backend-systemd-user.md` |
| 2026-04-14 | macOS `live-runtime` 仍缺 `launchd` 正式 backend | 本轮按设计决策先把 Linux / `Remote SSH` 的正式主路径收口到 `systemd-user`，macOS 仍停留在 `legacy-detached` fallback，没有等价的用户服务层 backend。 | macOS 上关闭 VS Code 后继续持有真实 `Agent` / `Terminal` 会话的可信度，以及后续三平台 runtime persistence 口径的一致性。 | 当前文档明确把 macOS 记为 fallback / `best-effort`，不对其宣称强保证 `live-runtime`。 | 下一轮需要补 macOS 正式 backend，或准备做三平台发布口径收口时 | `src/panel/runtimeHostBackend.ts`、`docs/design-docs/runtime-persistence-and-session-supervisor.md`、`docs/exec-plans/completed/runtime-host-backend-systemd-user.md` |
| 2026-04-13 | Agent 运行态判定仍缺 provider 原生权威事件与来源标注 | 本轮已把 CLI PTY 路径上的 `running / waiting-input` 误判从固定静默时间升级为组合启发式，并统一到 local PTY 与 runtime supervisor；但当前仍未接入 `Codex app-server`、`Claude Code` hooks/SDK/headless 等 provider 原生结构化事件，也还没有把节点状态来源和权威性写入 metadata。 | `Agent` 状态对外语义的可解释性与长期稳定性，尤其影响等待审批、provider UI 版本变化、结构化 sidecar 可用后与 PTY fallback 的优先级切换，以及 UI/诊断区分“权威事件”与“best-effort 推断”的能力。 | 当前保留 CLI PTY 为主执行通道，只在真实提交后进入 `running`，并综合 prompt、`OSC 9/777`、bell、行边界、spinner/redraw 与 hard fallback 判断回到 `waiting-input`；设计文档和 ExecPlan 已明确把这条路线标记为 fallback，而不是正式权威状态机。 | 下一轮准备接入 `app-server` / provider 原生结构化事件，或需要在 UI/诊断中展示状态来源与权威性时，应优先落这项技术债。 | `src/common/agentActivityHeuristics.ts`、`src/panel/CanvasPanelManager.ts`、`src/supervisor/runtimeSupervisorMain.ts`、`docs/design-docs/agent-running-state-detection.md`、`docs/exec-plans/completed/agent-running-state-detection.md` |
| 2026-04-12 | Codex fresh start session id 当前仍依赖启发式反查 | `Codex` 当前缺少可确认的标准 machine-readable session-id 获取接口；为避免继续使用 `resume --last`，本轮临时参考 OpenCove 的总体思路，在 fresh start 后扫描 `~/.codex/sessions/.../rollout-*.jsonl`，按 `cwd + 启动时间窗 + 候选唯一` 回填 session id。 | `Codex` 自动恢复能力的稳定性与可解释性，尤其影响跨版本兼容、私有状态格式漂移、时间窗歧义和真实节点身份绑定可信度。 | 当前显式把这条路线标记为技术债务 fallback，不当成正式 capability；实现上只要 miss、歧义或超时就 fail closed，不升级成 `resume-ready`，并保留 `codex resume <session-id>` 作为唯一正式恢复入口。 | OpenAI Codex CLI 提供标准 session-id 读取/注入接口后，或准备做真实 provider 恢复能力收口前，应优先移除这段启发式反查并改成正式接口。 | `src/common/codexSessionIdLocator.ts`、`src/panel/CanvasPanelManager.ts`、`src/supervisor/runtimeSupervisorMain.ts`、`docs/design-docs/agent-cli-launch-context-and-resume.md`、`docs/exec-plans/completed/agent-cli-launch-context-and-resume.md` |
| 2026-04-12 | 执行节点缩放命中修复当前依赖 `xterm.js` 私有内部接口 | 为在“终端仍随 React Flow 缩放”的产品定义下修复鼠标命中错位，本轮直接补到了 `MouseService`、`SelectionService` 和 textarea 定位相关的内部实现；这些入口不属于稳定公开 API。 | 后续升级 `@xterm/xterm` 时的兼容性，尤其影响拖选、越界滚动、右键定位与可能共用同一内部路径的鼠标上报行为 | 当前通过 Playwright harness 和真实宿主人工验证把缩放拖选、越界滚动边界与右键 textarea 定位都纳入回归，并把依赖路径显式记录在设计文档与完成计划中 | 下一轮升级 `@xterm/xterm`、整理共享适配层，或准备做更深的执行节点交互收口时 | `src/webview/main.tsx`、`tests/playwright/webview-harness.spec.mjs`、`docs/design-docs/execution-node-zoom-interaction-surface.md`、`docs/exec-plans/completed/execution-node-zoom-coordinate-alignment.md` |
| 2026-04-11 | Remote SSH 长断开仍缺 nightly / self-hosted 自动化回归 | 当前仓库已经补上 `systemd-user` backend、`fake-systemd` 本地 reopen smoke 和 backend / guarantee 断言，可以稳定覆盖“backend 选择是否正确”和“本地窗口重开后是否仍附着原会话”；但真正的 Remote SSH 长断开仍依赖远端 VS Code Server 生命周期、SSH 断链时长和 `systemd --user` 环境，普通 PR runner 不适合承担这条验证。 | Linux + Remote SSH 下“长时间断开后仍稳定 reattach”的对外可信度，尤其是用户关心的数分钟到更长时长断链场景 | 当前把这条验证明确留在技术债里，不再要求每个 PR 依赖人工断开回归；PR 层先跑本地 `fake-systemd` reopen smoke 与 fallback 验证，真实 Remote SSH 长断开留给后续 nightly / self-hosted。 | 下一轮补自托管 Linux runner 或受控 Remote SSH fixture，并把 `remote-ssh-real-reopen` 扩成可配置长断开时长的 nightly 任务时 | `scripts/run-vscode-smoke.mjs`、`scripts/vscode-remote-ssh-fixture.mjs`、`tests/vscode-smoke/real-reopen-tests.cjs`、`docs/design-docs/runtime-persistence-and-session-supervisor.md`、`docs/exec-plans/completed/runtime-host-backend-systemd-user.md` |
| 2026-04-08 | 真实 provider CLI 的 resume 主路径仍缺 end-to-end 本机验证 | 本轮已完成 `Agent` / `Terminal` 差异化生命周期、创建即自动启动和 `Agent` best-effort resume；自动化验证通过 fake-provider、smoke 与 Playwright 覆盖了状态流，但当前环境没有可直接验证的 `codex` 二进制，`Claude Code` 也只确认了 CLI 参数形态，没有在 smoke 里跑真实恢复。 | `Agent` 恢复能力对真实 `Codex` / `Claude Code` provider 的对外可信度，尤其是扩展重载后的恢复声明 | 当前把真实 provider 恢复明确写成 best-effort；自动化验证只把 fake-provider 恢复主路径、协议状态流和缺命令失败提示写成已覆盖，不把真实 provider end-to-end 写成已验证。 | 下一轮拿到真实 `Codex` / `Claude Code` 可执行环境，或准备发布前做 provider 级人工 / 自动化验收时 | `src/panel/CanvasPanelManager.ts`、`tests/vscode-smoke/fixtures/fake-agent-provider`、`tests/vscode-smoke/extension-tests.cjs`、`docs/design-docs/execution-lifecycle-and-recovery.md`、`docs/exec-plans/completed/execution-lifecycle-recovery-and-autostart.md` |
| 2026-04-06 | Linux `VSIX` smoke 当前只验证“打包内容完整性”，还不是完整安装态 | 为了先把打包产物是否可启动这条验证链落地，本轮的 `test:vsix-smoke` 采用“生成 VSIX -> 解包 -> 用解包产物跑 trusted smoke”的方式；它验证的是打包文件集是否齐全，而不是 `code --install-extension` 后的完整安装态。 | 发布工件验证的覆盖面，尤其是 VS Code 扩展安装流程、升级覆盖与安装后元数据行为 | 当前明确把它写成 packaged-payload smoke，不把这条验证包装成完整安装矩阵。 | 下一轮如果要在发布前收口真实安装态验证时 | `scripts/run-vscode-vsix-smoke.mjs`、`scripts/package-vsix.mjs`、`CONTRIBUTING.md`、`docs/design-docs/development-debug-automation.md` |
| 2026-04-06 | 自动化验证矩阵当前仍主要停留在 Linux，本轮未跑 macOS / Windows | 用户已明确把三平台矩阵延后到准备开源发布到 GitHub 时再做；本轮优先完成 Linux 上最值钱的 trusted / restricted smoke、Playwright UI 回归和 packaged-payload smoke。 | 发布前的平台可用性声明，尤其是 macOS / Windows 上的 PTY、Webview 容器行为和 VSIX 安装态 | 当前把三平台矩阵留作显式技术债，不把 Linux 验证写成全平台结论。 | 准备公开 GitHub 发布前，或需要做正式三平台发布验收时 | `tests/vscode-smoke/extension-tests.cjs`、`scripts/run-vscode-smoke.mjs`、`scripts/run-vscode-vsix-smoke.mjs`、`docs/design-docs/development-debug-automation.md` |
| 2026-04-06 | Remote-SSH 下的 debug profile 首次仍需本机人工准备 | 按 VS Code 官方设计，Remote 扩展调试应使用预装了 Remote Development 扩展的命名 profile；但当前仓库运行在远端，无法直接替用户修改本机 VS Code profile。 | 新机器或新环境第一次做 F5 调试时的初始化成本 | 当前在 `CONTRIBUTING.md` 中明确提供本机 CLI / GUI 的一次性准备步骤，并把 `Dev Session Canvas` 不应安装到 debug profile 写成显式约束。 | 如果后续找到稳定、官方支持的本机 profile 导入或预配链路时 | `CONTRIBUTING.md`、`.vscode/launch.json`、`docs/design-docs/development-debug-automation.md`、`docs/exec-plans/completed/extension-debug-automation.md` |
| 2026-04-06 | Webview UI 回归仍主要运行在浏览器 harness，真实容器覆盖面偏窄 | 为了把第三层先往真实 VS Code Webview 容器下压一格，本轮已经在 `@vscode/test-electron` smoke 中加入 test-only DOM probe；后续又补了真实容器里的 Note 编辑、provider 切换后重启、删除按钮，以及 pending request / stop 竞态 fault injection，但大多数 UI 回归仍由浏览器 harness 承担，而不是真实容器里的端到端交互。 | Webview UI 对真实容器特性的依赖，例如 CSP、容器级样式差异、真实宿主注入环境、指针命中细节与少量可访问性行为 | 当前用真实宿主 smoke 中的 probe 与 DOM action 覆盖节点渲染、Task/Note 写路径、provider 切换、删除按钮和两条生命周期 fault injection，并让 Playwright harness 继续承担截图基线、更多细粒度 UI 交互和页面级失败诊断；不把这套组合写成“已完全等价于真实容器 UI 自动化”。 | 下一轮如果要覆盖更深的 Webview 容器差异、复杂指针交互或发布前做 UI 风险收口时 | `src/common/protocol.ts`、`src/panel/CanvasPanelManager.ts`、`src/webview/main.tsx`、`tests/playwright/harness/webview-harness.html`、`tests/playwright/webview-harness.spec.mjs`、`tests/vscode-smoke/extension-tests.cjs`、`docs/design-docs/development-debug-automation.md`、`docs/exec-plans/completed/test-automation-hardening.md` |
| 2026-03-30 | 新建节点避碰当前依赖默认窗口尺寸估算 | 为先修复“新增节点初始重叠”的真实反馈，本轮宿主使用 `Agent` / `Terminal` / `Task` / `Note` 的默认窗口尺寸估算做矩形碰撞判断，而不是读取节点渲染后的真实边界。 | 新建节点默认摆放的精确性，尤其在后续调整节点样式、高度或字段密度后 | 当前让 Webview 提供当前视口锚点，宿主再基于统一尺寸估算做避碰；足以解决当前反馈中的初始遮挡，但不把它写成完整自动布局系统。 | 下一轮画布布局或节点尺寸模型收口时 | `src/common/protocol.ts`、`src/panel/CanvasPanelManager.ts`、`src/webview/main.tsx`、`docs/design-docs/canvas-feedback-polish.md`、`docs/exec-plans/completed/canvas-feedback-polish.md` |
| 2026-03-30 | 节点删除当前不支持确认或撤销 | 为先闭合四类节点的最小删除主路径，本轮直接提供节点头部删除按钮和键盘删除，但没有确认弹窗、撤销栈或回收站。 | 误删节点后的恢复体验，尤其影响包含正文的 `Task` / `Note` 和运行中的执行型节点 | 当前通过显式选中态、输入焦点保护、危险态按钮样式和单节点删除范围降低误触风险，不把确认/撤销写成已支持。 | 下一轮画布交互增强或对象历史能力设计时 | `src/webview/main.tsx`、`src/webview/styles.css`、`docs/design-docs/canvas-node-deletion.md` |
| 2026-03-30 | `node-pty` 后端仍缺 macOS / Windows / Remote 场景人工验证 | 为了把执行会话从 Linux 原型收口到统一 PTY 路线，本轮已把宿主切到 `node-pty`，并去掉了 Linux 平台硬拦截。代码路径已接通，但当前只有 Linux 构建与 PTY smoke test 证据。 | macOS、Windows、Remote SSH / Codespaces 下的 `Terminal` / `Agent` 可用性声明 | 当前只把 Linux smoke test 与构建结果写成已完成，其余平台继续标记为“验证中”。 | 下一轮平台验证与发布前人工验收时 | `src/panel/CanvasPanelManager.ts`、`src/panel/executionSessionBridge.ts`、`src/webview/main.tsx`、`docs/design-docs/execution-session-platform-compatibility.md` |
| 2026-03-30 | Windows 下 provider CLI 命令解析仍依赖显式命令路径验证 | `Agent` 节点当前直接以配置项中的命令路径启动 `codex` / `claude`；在 Windows 上，这可能涉及 `.cmd` / `.exe` 包装与 PATH 差异。 | Windows 下 `Agent` 节点的启动成功率与默认配置体验 | 当前继续保留设置项覆盖命令路径，并在缺命令时给出更明确的提示，不把默认 PATH 解析写成已验证。 | 下一轮 Windows 人工 smoke test 或命令解析增强时 | `src/panel/CanvasPanelManager.ts`、`docs/design-docs/execution-session-platform-compatibility.md` |
| 2026-03-30 | VSIX 当前会打入超出运行时必需范围的 `node-pty` 文件 | 为了先保证扩展可打包并带上原生 PTY 运行时，本轮通过 `.vscodeignore` 允许 `node-pty` 进入 VSIX；但当前产物仍包含 `deps/`、`build/` 元数据和部分非运行时文件。 | 扩展包体积、发布工件可审查性，以及后续发布时的冗余依赖面 | 当前优先保证 VSIX 可成功生成和运行时文件可用，把“进一步收紧到最小必需集”留作后续收口。 | 下一轮发布前瘦身或引入专门打包 staging 流程时 | `.vscodeignore`、`package.json`、`scripts/build.mjs`、`docs/exec-plans/completed/execution-session-platform-compatibility.md` |
